apiVersion: v1
kind: Service
metadata:
  name: gitea-service
spec:
  selector:
    app: gitea
  ports:
    - name: http
      protocol: TCP
      port: 3000
      nodePort: 30000
    - name: ssh
      protocol: TCP
      port: 2222
      nodePort: 30001
  type: NodePort
